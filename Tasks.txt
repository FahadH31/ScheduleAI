- Delete recurring events in one command.

- Polish UI/Frontend for main application page.

- Store chat history in session storage.

- Reload calendar automatically once openai updates the calendar.

- Add streaming & conversation history for openai chat portion.

- Display via IFrame & Public Calendar. Get user's account dynamically.


Future Potential:
- Voice chat 
- Support for tasks
- Support for calendars other than primary


Deleting Event Bug:
- Seems to only happen (sometimes) to events not created during the current session/auth. 
For example if I log out and back in. Doesn't occur if I create an event straight from the GC app.
Doesn't happen to updates.